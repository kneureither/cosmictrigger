//// Created by Konstantin Neureither on 26.06.20.//#include "slimSegsDataScript.h"#include <iostream>#include <vector>#include "utilityFunctions.h"int main(int argc, char *argv[]) {    /**     * Read data from MC Mu3e Simulation output .root files, preprocess the data and combine them into one slimmed dataset.     */    std::vector<int> runs = {1,2,3,4,5/*, etc*/}; //background file    // choose dataset and if to append the above runs.    // append=true is useful to append to an existing dataset file.    const int dataset = 1;    bool append = true;    for(auto it = std::begin(runs); it != runs.end(); ++it) {        std::cout << "STATUS : adding run " << *it << " to dataset " << dataset << std::endl;        slimSegsDataScript(dataset, *it, append);        append = true;    }}